import { AccountTypeAccordianTextPipe } from './account-type-accordian-text.pipe';

describe('AccountTypeAccordianTextPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountTypeAccordianTextPipe();
    expect(pipe).toBeTruthy();
  });
});
